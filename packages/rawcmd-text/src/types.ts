/**
 * Text alignment options.
 */
export enum TextAlign {
  LEFT = 'left',
  CENTER = 'center',
  RIGHT = 'right',
}

/**
 * List of available font styles.
 */
export enum TextStyle {
  RESET = 'reset',
  BOLD = 'bold',
  DIM = 'dim',
  ITALIC = 'italic',
  UNDERLINE = 'underline',
  INVERSE = 'inverse',
  HIDDEN = 'hidden',
}

/**
 * List of available font colors.
 */
export enum TextColor {
  BLACK = 'black',
  RED = 'red',
  GREEN = 'green',
  YELLOW = 'yellow',
  BLUE = 'blue',
  MAGENTA = 'magenta',
  CYAN = 'cyan',
  WHITE = 'white',
}

/**
 * List of available background colors.
 */
export enum TextBackground {
  BLACK = 'bgblack',
  RED = 'bgred',
  GREEN = 'bggreen',
  YELLOW = 'bgyellow',
  BLUE = 'bgblue',
  MAGENTA = 'bgmagenta',
  CYAN = 'bgcyan',
  WHITE = 'bgwhite',
}
